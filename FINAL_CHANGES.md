# KAMF PWA final adjustments

- Performance detail overlay is centered on the viewport instead of attached to the bottom.
- Added more vertical spacing between performance type badge and artist/team title.
- Invited artist detail overlays include YouTube Music and Spotify links.
- Home hero copy changed to `Now playing: KAMF` ordering.
- Current performance is calculated from `startAt` / `endAt` against the device clock, surfaced on Home, and highlighted as ON AIR on the schedule page.
- Schedule page automatically focuses the current performance's tab/date once when a live performance exists.
- KAMF contact channels updated to `@kaist_kamf` and `2024kamf@gmail.com`.
- Map label collision fixed: `창의학습관 (신학관)` is moved to the right edge vertically, and the nearby `학술문화관 방향` label is shifted right.
